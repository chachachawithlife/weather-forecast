let currentWeather = { 
    "temperature": 30,
    "condition": "sunny",
};

console.log(currentWeather);

let curWeather = `It's ${currentWeather.temperature} degrees F and ${currentWeather.condition}`; 

console.log(curWeather);